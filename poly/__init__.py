"""poly — a minimal CLI for placing buy/sell orders on Polymarket.

Built on the official unified ``polymarket-client`` SDK (``Polymarket/py-sdk``),
which trades from the deterministic deposit wallet by default and is safe against
the float-precision and deposit-wallet bugs of the legacy ``py-clob-client-v2``.
"""

__version__ = "0.1.0"
